{
  "name": "@jenix/cap-face-liveness",
  "version": "0.1.0",
  "description": "EdgeFolio-only: native camera liveness check (blink/head-turn) + MobileFaceNet embedding capture. Ported from APK/android's LivenessDetector.kt/CameraXManager.kt/FaceEmbeddingEngine.kt — do not add to EMS/Plugins, this is not a FieldForce plugin.",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "android/",
    "dist/"
  ],
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "test": "vitest run src/similarity.test.ts"
  },
  "peerDependencies": {
    "@capacitor/core": ">=7.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "capacitor": {
    "android": {
      "src": "android"
    }
  }
}
