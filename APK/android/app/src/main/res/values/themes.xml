<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Edgefolio" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme — switches to main theme after launch -->
    <style name="Theme.Edgefolio.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#1565C0</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Edgefolio</item>
    </style>
</resources>
